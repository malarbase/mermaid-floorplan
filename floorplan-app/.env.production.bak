# Production Environment Configuration Template
# ==============================================
# Copy this to .env.local and fill in your production values.
# NEVER commit .env.local - it contains secrets!

# Convex Cloud Configuration
# Get these from: https://dashboard.convex.dev
VITE_CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOYMENT=prod:your-project-name

# Better Auth
# CRITICAL: Generate a strong random secret for production!
# openssl rand -base64 32
BETTER_AUTH_SECRET=CHANGE-ME-IN-PRODUCTION-USE-STRONG-RANDOM-SECRET
BETTER_AUTH_URL=https://your-domain.com

# Google OAuth (Required for production)
# Get these from: https://console.cloud.google.com/apis/credentials
# Add redirect URI: https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-production-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-production-client-secret

# Development Auth Bypass (MUST be false in production!)
DEV_AUTH_BYPASS=false

# Mock Mode (MUST be false in production!)
VITE_MOCK_MODE=false

# Node Environment
NODE_ENV=production

# Super Admin Configuration (Optional)
# Set the email of the initial super admin user
SUPER_ADMIN_EMAIL=admin@your-domain.com
