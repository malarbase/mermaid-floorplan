# Development Environment Configuration
# =====================================
# This file contains defaults for local development with self-hosted Convex.
# Override any values in .env.local (which is gitignored).

# Convex Self-Hosted Configuration (Docker)
VITE_CONVEX_URL=http://localhost:3210
CONVEX_SELF_HOSTED_URL=http://convex:3210
CONVEX_SELF_HOSTED_ADMIN_KEY=local-dev|019a5fc49ab8fc332224e1623ff0af38b76215dd69ea731750c6403907a2a41148ac3ec4e6
CONVEX_DEPLOYMENT=dev:local

# Better Auth
# Generate a random secret: openssl rand -base64 32
BETTER_AUTH_SECRET=dev-secret-change-in-production-must-be-32-chars-long
BETTER_AUTH_URL=http://localhost:3000
SITE_URL=http://localhost:3000
VITE_CONVEX_SITE_URL=http://localhost:3211

# Google OAuth (Optional - only needed if testing real OAuth)
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Development Auth Bypass (Recommended for local testing)
# Set to 'true' to skip OAuth and use mock sessions
DEV_AUTH_BYPASS=true
DEV_USER_EMAIL=dev@example.com
DEV_USER_NAME=Dev User
DEV_USER_USERNAME=devuser

# Mock Mode (Optional - use mock data instead of real Convex)
# Set to 'true' to test without Convex backend, 'false' to use real Convex
VITE_MOCK_MODE=false

# Node Environment
NODE_ENV=development
