# Admin Testing Configuration Template
# =====================================
# Copy this to .env.local for admin feature testing
# Usage: cp .env.local.admin-example .env.local

# Enable development auth bypass with admin credentials
DEV_AUTH_BYPASS=true
DEV_USER_EMAIL=admin@test.local
DEV_USER_NAME=Test Admin
DEV_USER_USERNAME=testadmin

# Use real Convex (not mock mode) for full admin feature testing
VITE_MOCK_MODE=false

# NOTE: Make sure to set SUPER_ADMIN_EMAIL in Convex:
# cd floorplan-app && npx convex env set SUPER_ADMIN_EMAIL "admin@test.local"
