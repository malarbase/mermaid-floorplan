# Environment Configuration Template
# ===================================
# This is a comprehensive example showing all available configuration options.
#
# SETUP INSTRUCTIONS:
# 1. For local development: Use .env.development (already configured)
# 2. For production: Copy .env.production to .env.local and fill in secrets
# 3. For admin testing: Copy .env.local.admin-example to .env.local
#
# File Priority (higher overrides lower):
# .env.local > .env.development/.env.production > .env.example
#
# NEVER commit .env.local - it's for secrets and personal overrides!

# =============================================================================
# Convex Configuration
# =============================================================================

# Development (Self-Hosted in Docker)
VITE_CONVEX_URL=http://localhost:3210
CONVEX_SELF_HOSTED_URL=http://convex:3210
CONVEX_SELF_HOSTED_ADMIN_KEY=local-dev|your-admin-key-here
CONVEX_DEPLOYMENT=dev:local

# Production (Cloud)
# VITE_CONVEX_URL=https://your-project.convex.cloud
# CONVEX_DEPLOYMENT=prod:your-project-name

# =============================================================================
# Authentication Configuration
# =============================================================================

# Better Auth Secret
# Generate with: openssl rand -base64 32
# CRITICAL: Use a strong random secret in production!
BETTER_AUTH_SECRET=your-random-32-character-secret-key-here
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Remember to add redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# =============================================================================
# Development Auth Bypass (Local Testing Only)
# =============================================================================

# Set to 'true' to skip OAuth and use mock sessions
# MUST be 'false' in production!
DEV_AUTH_BYPASS=true
DEV_USER_EMAIL=dev@example.com
DEV_USER_NAME=Dev User
DEV_USER_USERNAME=devuser

# =============================================================================
# Mock Mode (Optional)
# =============================================================================

# Set to 'true' to use mock data instead of real Convex
# Useful for testing UI without a Convex backend
# Set to 'false' to use real Convex (recommended for admin/editor testing)
VITE_MOCK_MODE=false

# =============================================================================
# Admin Configuration (Optional)
# =============================================================================

# Email of the initial super admin user
# Set this in Convex: npx convex env set SUPER_ADMIN_EMAIL "your@email.com"
SUPER_ADMIN_EMAIL=admin@your-domain.com

# =============================================================================
# Node Environment
# =============================================================================

NODE_ENV=development
